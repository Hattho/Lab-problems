#include <stdio.h>
#include <stdlib.h>

#define multiply(x, y) x*y;

int x,y;

int main () {
    scanf("%d", &x);
    scanf("%d", &y);

    int p=multiply(x, y)

    printf("%d ", p);

    return 0;

}

